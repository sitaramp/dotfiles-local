#!/bin/bash
# full update the git repo that you are sitting in..

git pull

#First time or when new submodule are adede
git submodule update --init --recursive --remote

# update repo and submodules
#git submodule update --recursive --remote
